# 从夯到拉 - 多领域评价平台

一个让用户对各种领域内容进行分层次评价的Web应用，基于"从夯到拉"的七级评价体系。

## 🌟 特色功能

- **七级评价体系**：从"拉"到"顶"的完整等级划分
- **多领域支持**：游戏、影视、音乐、文学、美食、科技等
- **多维度评价**：总体、技术、创意、执行、影响力
- **用户系统**：注册、登录、评价历史
- **实时统计**：动态更新评价数据和平均值
- **响应式设计**：支持移动端和桌面端

## 📋 评价等级说明

| 等级 | 名称 | 分数 | 描述 |
|------|------|------|------|
| lā | 拉 | 1 | 表现较差 |
| hāng | 夯 | 2 | 基础水平 |
| zhōng | 中 | 3 | 平均水平 |
| shàng | 上 | 4 | 较好表现 |
| jiā | 佳 | 5 | 优秀水平 |
| rénshàng | 人上 | 6 | 超越大多数 |
| dǐng | 顶 | 7 | 顶级表现 |

## 🛠️ 技术栈

- **后端**：Node.js + Express.js
- **数据库**：SQLite3
- **前端**：原生JavaScript + HTML5 + CSS3
- **认证**：JWT (JSON Web Tokens)
- **密码加密**：bcryptjs
- **样式**：自定义CSS + Font Awesome图标

## 🚀 快速开始

### 1. 环境要求
- Node.js (v14 或更高版本)
- npm 或 yarn

### 2. 安装步骤

```bash
# 克隆项目
git clone https://github.com/QiyuanY/rating-platform-demo.git
cd rating-platform-demo

# 安装依赖
npm install

# 复制环境配置文件
cp .env.example .env

# 启动服务器
npm start
```

### 3. 访问应用
打开浏览器访问：`http://localhost:3000`

### 4. 示例账户
项目启动后会自动创建示例用户：
- 用户名：`demo_user` / `gamer_pro` / `movie_fan`
- 密码：`123456`

## 📚 API 文档

### 认证接口

#### POST /api/auth/register
用户注册
```json
{
  "username": "testuser",
  "email": "test@example.com",
  "password": "password123"
}
```

#### POST /api/auth/login
用户登录
```json
{
  "username": "testuser",
  "password": "password123"
}
```

### 内容接口

#### GET /api/contents
获取内容列表
```
Query参数：
- category: 分类筛选 (可选)
- page: 页码 (默认1)
- limit: 每页数量 (默认20)
```

#### POST /api/contents
创建内容 (需要认证)
```json
{
  "title": "内容标题",
  "category": "game",
  "description": "内容描述",
  "image_url": "图片链接"
}
```

### 评价接口

#### POST /api/ratings
提交/更新评价 (需要认证)
```json
{
  "content_id": "内容ID",
  "overall_level": "jiā",
  "technical_level": "shàng",
  "creativity_level": "jiā",
  "execution_level": "zhōng",
  "impact_level": "rénshàng",
  "comment": "详细评价",
  "tags": ["优秀", "推荐"]
}
```

#### GET /api/contents/:id/ratings
获取内容的评价列表

#### GET /api/my-ratings
获取当前用户的评价历史 (需要认证)

### 统计接口

#### GET /api/stats
获取平台统计数据
```json
{
  "totalUsers": 100,
  "totalContents": 50,
  "totalRatings": 200,
  "categoryStats": [
    {"category": "game", "count": 20},
    {"category": "movie", "count": 15}
  ]
}
```

## 🎨 使用指南

### 1. 用户注册/登录
- 点击右上角的用户信息区域进行登录
- 支持用户名或邮箱登录
- 新用户需要注册后使用

### 2. 浏览内容
- 使用顶部分类标签筛选不同领域的内容
- 查看每个内容的评价统计和平均分
- 点击内容卡片进行评价

### 3. 评价内容
- 选择总体评价（必选）
- 可选的技术、创意、执行、影响力等维度评价
- 添加详细评价文字
- 使用标签标记评价要点

### 4. 创建内容
- 点击右下角的"+"按钮
- 填写标题、选择分类、添加描述
- 可选择添加图片链接

### 5. 查看统计
- 顶部统计面板显示平台总体数据
- 每个内容显示平均分和评价人数
- 评价分布可视化

## 📁 项目结构

```
rating-platform-demo/
├── public/              # 前端文件
│   ├── index.html      # 主页面
│   ├── styles.css      # 样式文件
│   └── app.js          # 前端逻辑
├── server.js           # 主服务器文件
├── package.json        # 项目配置
├── .env.example        # 环境变量示例
└── README.md           # 项目文档
```

## 🔧 自定义配置

### 修改端口
编辑 `.env` 文件：
```
PORT=8080
```

### 修改JWT密钥
编辑 `.env` 文件（生产环境请使用强随机密钥）：
```
JWT_SECRET=your-super-secret-key
```

### 添加新的内容分类
在 `server.js` 中修改分类定义：
```javascript
// 在前端页面中添加新的分类按钮
// 在后端API中添加相应的验证逻辑
```

## 🧪 开发调试

### 开发模式
使用 nodemon 进行热重载开发：
```bash
npm run dev
```

### 数据库管理
SQLite数据库文件：`rating_platform.db`
- 使用任何SQLite客户端查看数据
- 备份：复制数据库文件
- 重置：删除数据库文件，服务器重启时会重新创建

## 🚀 部署指南

### 本地开发部署
1. 按照快速开始步骤操作
2. 确保3000端口未被占用

### 生产环境部署
1. 使用 PM2 管理进程：
```bash
npm install -g pm2
pm2 start server.js --name "rating-platform"
```

2. 配置反向代理（Nginx）：
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

3. 设置环境变量：
```bash
# 生产环境建议设置
NODE_ENV=production
JWT_SECRET=your-production-secret
```

## 🐛 常见问题

### Q: 端口被占用
A: 修改.env文件中的PORT配置，或关闭占用3000端口的程序

### Q: 数据库错误
A: 删除rating_platform.db文件，重新启动服务器会自动创建

### Q: 前端页面无法访问
A: 确保public目录下的文件完整，特别是index.html

### Q: 登录失败
A: 检查用户名/邮箱和密码，或尝试使用示例账户

## 📄 开源协议

MIT License - 详见 LICENSE 文件

## 🤝 贡献指南

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📞 联系方式

- 项目地址：https://github.com/QiyuanY/rating-platform-demo
- 问题反馈：https://github.com/QiyuanY/rating-platform-demo/issues

---

⭐ 如果这个项目对您有帮助，请给它一个星标！